# 測試稽核 — 未定案項目

本檔為 **2026-07-26 的一次性稽核決策存檔**(非持續維護的待辦清單);條目狀態以當日裁定為準,後續變更見各條註記。其中 13 條已於 2026-08-02 覆核仍成立,未改動內容;僅 L13 因前提被重構推翻而重新開放,見該條。

需要產品決策才能決定正確行為的項目。**在定案前不要替它挑一個行為並寫測試把它固化成契約。**

- [x] **已定案並落地(選 B + platform 第二道牆)** — platform `src/Platform.Service/Abstractions/ChatMemoryKeyDerivation.cs:25` — 記憶鍵以 `:` 串接(`{tenant}:{user}` + `:{conversationId}`)且不逃逸分隔符:身分或 conversationId 本身含 `:` 時,理論上 A 使用者的 cid 可與 B 使用者的 `uid:conversationId` 相撞(例:tenant `t`/user `a:b` 的 uid 與 tenant `t:a`/user `b` 的 uid 同為 `t:a:b`)。短期記憶視窗與 mem0 uid 都用這個鍵,撞鍵即跨使用者可見。兩個候選行為:(A) 在 `UserContext.IsolationKey`/`Derive` 逃逸或改用長度前綴編碼(風險:改變既有 mem0 uid 與 session key 格式 → 現有長期記憶與進行中的對話視窗全部斷掉,屬追溯破壞);(B) 在註冊/登入邊界禁止使用者名稱與租戶碼含 `:`,並補一條 backend 驗證測試(風險:既有資料若已存在含 `:` 的帳號需要遷移,且 platform 端仍無第二道牆)。**結案**:採 B 並補上它缺的第二道牆,鍵格式一個 byte 都沒動。(a) backend:`RegisterRequest.Username/TenantCode` 與 `LoginRequest.Username` 各掛 `[RegularExpression(IdentityRules.NoColon)]`(pattern `^[^:]*\z`,`[^:]` 含換行故不順手收緊既有允許值),回既有 400 ApiError + fieldErrors;這兩個值只有 register/login 兩個公開入口(租戶無建立 API,只有 DbBootstrap/InMemory 的硬編碼 `demo-a`/`demo-b` seed),兩個入口都擋。(b) platform:守門放在 `UserContext.IsolationKey`(鍵格式的單一真理來源)而不是 `ChatMemoryKeyDerivation`,因為 AG-UI 的 `JwtTenantIsolationKeyProvider` 也直接吃 `IsolationKey` —— 擋在共用處一次,兩條鏈路(`/api/chat*` 與 AG-UI)同時 fail closed(`InvalidOperationException` → 500)。只檢查身分欄位:匿名鏈路根本不進 `IsolationKey`,已被前綴的 conversationId 含 `:` 也照常,兩者都有測試釘住。
- [x] **已定案並落地(選 A)** — workflow `app/main.py:438`（`POST /skills/validate-package`）— 匯入路徑沒有套用「script 步驟僅限 ADMIN 撰寫」gate：`/skills/validate` 傳 `author_role=ctx.role`（`main.py:414`），`validate-package` 不傳，`package._parse_flow` → `validate_source(definition)`（`package.py:881`）也不帶；實測同一份含 `script:` 的 flow 定義，USER 走 validate 得到 `valid=false, forbidden_script`，走 validate-package 得到 `valid=true` — 兩個候選行為：(A) 匯入也帶 `author_role`（風險：若 backend 端已另有 role gate 則是重複把關，且既有以 USER 身分匯入的 package 匯入流程會開始失敗，屬追溯破壞）；(B) 明示「匯入刻意不 gate，由 backend 的寫入權限把關」並補一條測試釘住這個意圖（風險：workflow 端的信任邊界依賴呼叫端正確帶 role，一旦 backend 的 gate 退化就沒有第二道牆）。**結案**:採 A,實際做法 —— A 案原列的「既有以 USER 身分匯入的 package 會開始失敗」風險經查證不存在:backend 三個 validate-package 入口(`SkillController.cs:237/246/333`)與 platform 兩個 import proxy 全掛 `[AdminOnly]`,且 workflow 的 `/skills/validate-package` 是 internal-only(`security.py:19-42`,platform 無對應 proxy),USER 到不了這個端點;`WorkflowSkillPackageValidator.cs:64` 早已在轉發 `X-User-Role`,缺的只是消費端。作法:`parse_package` 加 keyword-only `author_role`,傳給 `_parse_flow` → `validate_source(definition, author_role=...)`,端點帶 `ctx.role`。runtime 載入既有 package 的兩條路徑(`artifacts.py:115/158`、`package_reader.py:47`)不帶 → gate 關閉,與 `custom.load`／invoke 一致,零追溯破壞。測試補在 `tests/test_validate_package_api.py`:USER+script → `forbidden_script`(先取紅)、ADMIN+script → valid(決策表另一半)、USER 無 script → valid。
- [x] **已定案並落地(選 A)** — workflow `app/runtime/write_evidence.py:54` — `if not isinstance(version, int) or version < 1` 把 JSON `true` 當成合法版本(Python 的 `bool` 是 `int` 子類,`True < 1` 為 False),Backend 回 `{"version": true}` 會被當成 version 1 接受並回傳 `True` 給 `write_evidence` 工具的 `int(result)`。兩個候選行為:(A) 收緊為 `isinstance(version, int) and not isinstance(version, bool)`(風險:若某個 Backend/測試替身確實回傳 bool,會從「接受」變成 `WriteEvidenceError`,屬行為變更);(B) 維持現狀並記錄「bool 版本視為 1」(風險:durable evidence 的版本號失去型別保證,錯誤的 Backend 回應不會 fail closed)。目前測試只釘住 `0`/字串/缺欄位/非物件四格,刻意不含 bool。**結案**:採 A,實際做法是把守門收緊成 `not isinstance(version, int) or isinstance(version, bool) or version < 1`,並在原地註明「`bool` 是 `int` 子類,JSON `true` 否則會被當成 version 1 回傳」。B 案的風險（durable evidence 版本號失去型別保證）不可接受;A 案的「若某個 Backend/測試替身確實回傳 bool 會轉為 `WriteEvidenceError`」實測不存在 —— 全套測試無任何替身回 bool。測試在既有的 `test_write_evidence_sink_fail_closed_matrix` 矩陣補兩格(`True` 與 `False`),`True` 那格先取紅。
- [x] **已定案並落地(選 A)** — frontend `src/ruleBuilder.ts:16` + `src/components/BusinessRuleEditor.tsx:318,487` — `MAX_RULE_UI_DEPTH = 3` 是硬編碼常數,catalog 回傳的 `limits.maxDepth`(`/api/agents/catalog/rule-facts`,由 workflow `app/business_rules/catalog.py:27` 提供)在 UI 完全不被讀取。目前兩邊都是 3 所以看不出來;伺服器放寬時前端會靜默擋住合法 authoring,而契約明寫「前端不是驗證權威」。已補 `frontend/tests/agentBuilder.ui.spec.ts` 的 `catalog-provided rule limits drive nesting depth instead of a hardcoded constant`(標 `test.fail()`,實測紅:catalog 給 maxDepth=5 時 depth 3 的 `類型` 選單 resolved to 0 elements)。兩個候選行為:(A) 讓 `BusinessRuleEditor` 讀 catalog 的 `limits.maxDepth`(缺席時 fallback 3)(風險:catalog 若回傳過大值會讓 UI 允許 authoring 出伺服器仍會擋的深度,且動到 D2 生產程式碼);(B) 維持硬編碼並明示「UI 巢狀上限刻意比伺服器嚴格」,把測試改成釘住 3(風險:兩邊各自維護同一個數字、無自動偵測 drift 的機制)。**結案**:採 A,實際做法是把常數換成 `ruleUiDepthLimit(factCatalog)`(`src/ruleBuilder.ts`),直接讀 `RuleCatalogEnvelope.limits.maxDepth`,`ConditionEditor` 用它取代兩處硬編碼比較;catalog 缺席、非 envelope(陣列)、`limits` 缺席、或值不是正整數(`0`/`NaN`/小數)一律 fail-safe 回落 3,絕不會退化成 0 層。A 案已知取捨(catalog 給過大值時 UI 允許 authoring 出伺服器仍會擋的深度)刻意不加前端上限——server validate/publish 才是權威。`test.fail()` 標註已移除,該測試靠生產碼修正轉綠;另補 `agentBuilder.unit.spec.ts` 的 `nesting depth follows the catalog limit and fails safe to 3, never to 0` 釘住 fallback 分支。
- [x] **已定案並落地(選 B)** — backend `src/Backend.Api/Skills/WorkflowSkillValidator.cs:104` vs `WorkflowSkillPackageValidator.cs:150` — 空白 description 的 round-trip 破裂:definition-only 寫入允許 description 缺席(預設 `string.Empty`,`Valid_MissingRequiredRole_DefaultsToUser` 已釘住),`SkillExporter` 把它匯出成 `description: ""`,但同一份 zip 再走 import 會被 package validator 的「skill.description 必須是非空字串」判 502 —— 同一個 skill 匯得出來、匯不回去。兩個候選行為:(A) definition-only 寫入也要求非空 description(對齊 package validator,502/422)(風險:既有沒寫 description 的 flow skill 之後每次 PUT 都會失敗,屬追溯破壞);(B) package validator 放寬為允許空 description(風險:匯入路徑少一道契約檢查,Claude-Skill frontmatter 的 description 是給模型看的選用資訊,空值會一路流進 agentic 目錄)。**結案**:採 B,`WorkflowSkillPackageValidator.ValidateSuccessContract` 移除「description 必須是非空字串」該格,metadata 映射改 `Description ?? string.Empty`(與 definition-only 寫入 `WorkflowSkillValidator.cs:104` 同一個預設)。其餘契約格(name、expected_name、kind、required_role、canonical 一致性)一格未動。測試:`SkillPackageValidatorTests` 的違約 theory 移除 blank-description 那列(該列釘的正是本次要改掉的行為,不是放寬別的斷言),改補 `Valid_BlankOrMissingDescription_IsAccepted_AsEmptyString`(`""`/`null` 兩格);round trip 由 `SkillExportTests.Export_BlankDescription_RoundTripsBackThroughPackageImportValidation` 端到端背書 —— 走真的 export API 取 zip bytes,把 SKILL.md 的 frontmatter 當 YAML 解析(模擬匯入端)後把該 description 餵進真的 `WorkflowSkillPackageValidator`,而不是測試自己寫死一個空字串。
- [x] **已定案並落地(選 A)** — workflow `app/runtime/graph.py:1147` vs `:1149` — `_budget_error` 的比較子不對稱:step/token 用 `>=`、context_rounds 用 `>`,所以 `max_context_rounds=2` 實際允許 3 輪而 `step_budget=12` 只允許 11 步。兩個候選行為:(A) 統一成 `>=`(風險:既有以 `max_context_rounds` 設定的 Agent 會少一輪追問,屬行為變更且對進行中的 run 是靜默縮減);(B) 統一成 `>`(風險:所有預算實際上限各 +1,token 預算會超出既有計費假設)。定案前刻意不寫「剛好等於上限」的邊界測試,現有測試一律用「遠超上限」值(`test_run_budgets_fail_closed_before_the_model_runs`)。**結案**:採 A,`context_rounds` 改 `>=`,三個比較子一致,並在 `_budget_error` 註明「`max_*` 是對已消耗量的閉區間上限,用滿即停;三者必須保持一致,否則 `max_context_rounds=N` 會靜默允許 N+1」。已知代價(進行中的 run 少一輪追問)接受;`_policy_gate:515` 的 rule 追問路徑本來就是 `>=`,改後兩處語意一致。新增 `test_run_budget_boundaries_are_inclusive_upper_bounds`(三個預算各一組 on-point/off-point:12/11、2/1、4000/3999,以 `UsageModel(0)` 讓 token off-point 不撞 `model_usage_invalid`),六格中只有 `context-at-limit` 先取紅;既有的「遠超上限」測試原封不動照樣綠。
- [x] **已定案並落地(選 B)** — workflow `app/business_rules/decimal_value.py:56` — `decimal_precision_exceeded` 是不可達分支:整數位 ≤20、scale ≤18 兩道檢查在它之前,significant digits 最多 38,永遠不會 `> MAX_DECIMAL_PRECISION`。兩個候選行為:(A) 刪掉該檢查並在 catalog 註明 precision 由前兩個上限推導(風險:catalog 對 .NET/前端仍公告 `maxPrecision: 38`,移除後若日後放寬整數位/scale 就少一道獨立護欄);(B) 保留為防禦性檢查並加註「目前不可達」(風險:error code 永遠不會出現,消費端可能為它寫死無用的錯誤處理)。已測到可達的最大精度 38 必須通過(`test_decimal_scale_precision_and_leading_zero_boundaries`)。**結案**:採 B,保留檢查並在原地加註 —— 已覆核不可達理由仍成立(`:45` 整數位 ≤20、`:50` scale ≤18,兩者相加恰為 38 == `MAX_DECIMAL_PRECISION`)。註解寫明「哪個上游先擋掉」與「什麼改動會讓它變可達」(把 `MAX_DECIMAL_INTEGER_DIGITS + MAX_DECIMAL_SCALE` 調到大於 `MAX_DECIMAL_PRECISION` 時立即生效),因此 catalog 的 `maxPrecision: 38` 維持為獨立契約而非推導值。依定案不為死分支補測試。
- [x] **已定案並落地(選 B)** — workflow `app/runtime/policy.py:129-136` — `unpinned_rule_skill` 由公開建構路徑不可達:`PreActionPolicy.__init__` 把同一份 `pinned_skills` 當成 referenceCatalog 傳給 validator,未 pin 的 `route_to_skill` 在建構期就 `PolicyError`,`decide()` 永遠看不到。兩個候選行為:(A) 視為刻意的縱深防禦並註明不可達(風險:未來若讓 validator 與 pin 清單來源分離,沒有測試會發現守門失效);(B) 讓 validator 與 decide 使用不同來源(例如 validator 用租戶目錄、decide 用 snapshot pin)以真正啟用該分支(風險:兩份清單不同步時錯誤訊息會從建構期挪到執行期,較難診斷)。現有測試釘住的是建構期 fail-closed(`test_policy_rejects_rules_routing_to_an_unpinned_skill`)。**結案**:採 A(＝保留 + 註明),已覆核不可達理由仍成立 —— `__init__:50` 把同一份 `pinned_skills` 當 `skills` referenceCatalog 傳下去,`validator._reference_is_allowed`(`app/business_rules/validator.py:170`)把未 pin 的 `route_to_skill` 判成 `unknown_skill_reference`,建構期就 `PolicyError`;`skill` 參數 `required=True`,缺漏同樣在建構期被擋,連空字串都到不了 `decide()`。註解寫明上游守門位置與「validator 目錄與 runtime pin 清單一旦分離就變可達」。依定案不為死分支補測試。
- [ ] workflow `app/runtime/flow_harness.py`(前身 `app/runtime/legacy_flow.py:98-100`)— `tool_calls_bound > remaining_tool_rounds` 分支。**前提已於 `02dde09`("Unify governed workflow execution")被推翻**:`legacy_flow.py` 已整檔刪除,新建 `workflow/app/runtime/flow_harness.py`;新版 `_validate_steps`(`flow_harness.py:337-371`)不再無條件拒絕 tool step / `requires_tools` node,改為依 `effective_tools` 治理放行,且 `invoke_pinned_flow`(`flow_harness.py:255,261`)由 Harness 正式路徑呼叫(`workflow/app/runtime/graph.py:594` 的 `_invoke_business_workflow`)。因此 `tool_calls_bound` 現在可為非零,該分支不再保證不可達。同一 commit 新增的 `test_flow_harness.py::test_flow_budget_rejects_when_only_tool_bound_exceeds` 測的是 `invoke_flow_with_governance`(Harness-internal 治理路徑),**不是** `invoke_pinned_flow`(D3/D5 pinned-snapshot 路徑,即本條所指函式);`invoke_pinned_flow` 目前只有 `test_agent_runtime_flow.py:799-816` 的 `test_pinned_flow_admits_budgets_equal_to_its_bounds` 用零 tool-call 的 `flow_artifact()` 驗證「等於上限放行」,沒有任何測試驗證「tool 步驟造成 tool_calls_bound 超界 → budget_exhausted」。**待重新裁定**:是否為 `invoke_pinned_flow` 補一條 tool_calls_bound 超界的邊界測試。
- [x] **已定案並落地(選 A 的變體:repo 無 CI,改落地為文件)** — workflow `tests/test_agent_runtime_postgres.py` — 生產唯一支援的 checkpointer(`PostgresCheckpointStore`)在沒有 `CHECKPOINT_DATABASE_URL` 時整檔 skip,CI 預設從未執行過 `setup()`/pool 參數/HMAC 簽章路徑。兩個候選行為:(A) CI 加一個帶 DSN 的 job(風險:CI 需要 postgres service,build 時間與維運成本上升);(B) 維持 opt-in skip(風險:`AsyncPostgresSaver` 相容性與 `put/get_root_context_checkpoint` 的驗簽只在人工跑 evidence 時才被覆蓋)。本地以 `CHECKPOINT_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/springaitest` 實跑,兩個測試皆綠。**結案**:採 A 的前提不成立 —— 本 repo **完全沒有 CI 設定**(已用 Glob 掃過 `.github/`、`azure-pipelines*`、`.gitlab-ci*`、`.circleci`、`Jenkinsfile*`、`.travis*` 等,root 只有 `.claude/`、`.vscode/`、`infra/`、`scripts/`,唯一命中的都在 `frontend/node_modules/` 裡),因此不自行發明一套 CI。改在 `workflow/AGENTS.md` 的 Gotchas 補一條:寫明三支預設 skip 的測試(本檔 2 支 + `test_root_orchestrator_supervisor.py::test_root_checkpoint_round_trips_through_real_postgres_when_configured`)、它們遮蔽了什麼(`PostgresCheckpointStore.open()`/`setup()` 與 pool 參數、`AsyncPostgresSaver` 的 interrupt→重開→resume 相容性、`put/get_root_context_checkpoint` 的 HMAC 簽章與 payload digest 驗證)、以及要跑就設哪個環境變數與確切指令。本輪以該 DSN 實跑 `tests/test_agent_runtime_postgres.py tests/test_root_orchestrator_supervisor.py` = **14 passed, 0 skipped**。
- [x] **已定案並落地(選 A,收窄範圍)** — platform `src/Platform.Web/Controllers/SkillController.cs:79-80,89-90`(`[RequestSizeLimit]` + `[RequestFormLimits]` 17 MiB)— 超過 Web 層上限的匯入實測回 **400「輸入驗證失敗」**(`fieldErrors:{}`),不是 413:限制被 form model binding 攔下,`GlobalExceptionHandler.Map` 沒有 `BadHttpRequestException`/`InvalidDataException` 的分支,落到 `[ApiController]` 的自動 400。稽核報告假設是 413。兩個候選行為:(A) 在 `GlobalExceptionHandler` 加映射讓它回 413(風險:動到全域例外對照表,所有端點的 form/body 過大都會跟著改狀態碼,且 `WorkflowPayloadTooLargeException` 已佔用 413 語意需釐清訊息);(B) 明示「Web 層上限一律以 400 表達,413 保留給 Business Rule payload」並補測試釘住 400(風險:16 MiB 的 service 層檢查與 17 MiB 的 Web 層檢查對客戶端完全無法區分,前端無法提示「檔案太大」)。**結案**:採 A 但**未動全域 `GlobalExceptionHandler.Map`**(避免所有端點的 form/body 過大都跟著改狀態碼)。改在 `SkillController` 內以 private nested `PackageTooLargeAttribute`(`IResourceFilter`,早於 model binding、晚於所有 authorization filter,所以 USER 仍固定 403 而非 413)檢查 `Content-Length`,超限丟既有 `WorkflowPayloadTooLargeException` 走既有 413 映射;訊息「Skill 套件超過上傳大小上限（17 MiB）」與 Business Rule payload 那條不重疊。四處字面值收斂成 `PackageSizeLimitBytes`。測試收緊為 `Import_OverRequestSizeLimit_Returns413_BeforeReachingService`,精確斷言 413 + ApiError 形狀 + 不抵達 service 層。**結案**:採 A 但收窄到只有匯入路徑 —— **沒有動 `GlobalExceptionHandler.Map`**(改對照表會讓所有端點的 form/body 過大跟著改狀態碼)。作法是 `SkillController` 內一個 private nested `PackageTooLargeAttribute`(`IResourceFilter`),兩條 import 路由各掛一個:resource filter 早於 model binding(所以 400 不會先發生)、晚於所有 authorization filter(所以 USER 的超大上傳仍固定 403,既有測試不變),`Content-Length` 超限即丟既有的 `WorkflowPayloadTooLargeException` → 既有 413 映射 + 標準 ApiError,不新增例外型別也不新增 Map 分支。413 語意衝突問題以訊息區分:本條是「Skill 套件超過上傳大小上限(17 MiB)」,Business Rule payload 那條講的是 payload,兩者訊息互不重疊。四處 `17L * 1024 * 1024` 字面值收斂成 `PackageSizeLimitBytes` 一個常數。已知上限(已在原地標 `ponytail:`):只看 `Content-Length`,沒帶(chunked)時仍由 `[RequestSizeLimit]` 兜底成 400 —— 瀏覽器送 FormData 一定會帶。測試把原本的「非 2xx」收緊成 `Import_OverRequestSizeLimit_Returns413_BeforeReachingService`,精確斷言 413 + status/message/timestamp/空 fieldErrors,並擴成 Theory 覆蓋具名與 server-derived 兩條路由(漏掛其中一條會被抓到)。
- [x] **已定案並落地(選 A)** — platform `src/Platform.Web/Auth/AuthenticatedUser.cs:42-50`(`GetCapabilities`)— 與同檔 `GetGroups` 的上限不對稱:groups 有 `MaxGroups=256` + `MaxGroupsWireUtf8Bytes=2048` 兩道界限且整組 fail-closed,capabilities 只逐項過濾空白/含空白字元,數量與聚合長度完全無上限(`X-User-Capabilities` header 大小因此由簽發端決定)。兩個候選行為:(A) 比照 groups 加上數量/wire byte 上限並整組拒絕(風險:若 backend 未來合法簽發較多 capability,超限會讓已登入使用者整批權限消失,是 fail-closed 但無聲;且需與 backend 簽發端同步定上限);(B) 明示「capability 由 backend 簽發、天生小量,不設防禦上限」並補一條註解/測試釘住這個意圖(風險:header 大小無第二道牆,簽發端一旦退化 platform 不會擋)。定案前刻意不寫「capabilities 上限」的邊界測試(會把一個生產碼沒有的上限固化成契約)。**結案**:採 A,`GetCapabilities` 先看**原始** claim 陣列的數量與 `string.Join(' ', claims)` 的 UTF-8 位元組數,任一超限即整組回 `null`(與 `GetGroups` 同語意:不回一個看起來合法的子集),通過後才走既有的空白過濾/去重。上限沿用 groups 的數量級與命名風格:`UserCapabilityContract.MaxCapabilities = 256`、`MaxCapabilitiesWireUtf8Bytes = 2_048`,放在 `Platform.Service/Dtos/UserContext.cs` 內 `UserGroupContract` 的正上方,兩份 wire 界限常數並排。既有的逐項語意(空白/含空白字元的 claim 被濾掉、缺席回 null)一個字沒改,三條既有 `GetCapabilities_*` 測試原封不動照樣綠。測試補四格 on-point/off-point:數量 256 通過 / 257 整組拒絕、聚合位元組恰 2048 通過 / 超 1 byte 整組拒絕(重用既有的 `GroupSet` helper 產生剛好 2048 bytes 的集合),兩個「拒絕」格另外斷言 `HasCapability("workflow.manage")` 為 false。
- [x] **已定案並落地(選 A 的變體)** — backend `src/Backend.Api/Data/InMemory/InMemoryAgentRunApprovalRepository.cs:295-301`(`CompleteExecuteAsync(deadLetter:true)`)— lite 與 Dapper 行為分歧:Dapper(`AgentRunApprovalRepository.cs:236`)直接 `UPDATE agent_run SET status='failed', error_code='approved_write_unrecoverable'`,繞過一般轉移守門;lite 版改走 `TransitionAsync(current.StateVersion, Failed)`,缺 lease/checkpoint/ExpectedEventAckCursor 因此**必定失敗且回傳值被丟棄**,run 永遠停在 `queued`,下一輪不會有人再處理這筆已核准但無法完成的寫入。實測(本輪先寫的 `CompleteExecute_DeadLetter_FailsTheRun`)Expected "failed" / Actual "queued"。兩個候選行為:(A) lite 直接改寫 Entry 狀態成 failed + `approved_write_unrecoverable`,與 Dapper 對齊(風險:繞過 `TransitionAsync` 的所有不變量檢查,lite 從此有第二條寫狀態的路徑);(B) 讓 `CompleteExecuteAsync` 帶著執行 lease 走正規轉移(風險:dead-letter 發生時 lease 通常已經沒了,等於這條路徑常態失敗,需要先定義「無 lease 的系統性終結」語意)。**結案**:採 (A) 但避開了它的風險 —— 沒有在 lite 開第二條寫狀態的路徑,而是走既有的 `IAgentRunApprovalDecisionTransition` seam 新增 `FailApprovalRunAsync`,WHERE 條件與 Dapper `:236` 逐條對齊。同一支檔案的姊妹路徑 `ClaimExecuteLocked`(過期未執行)後續也一併對齊 Dapper 的第三段 UPDATE。
- [x] **已定案並落地(選 A,create-only)** — backend `src/Backend.Api/Agents/AgentController.cs:51`(`Require(request.Slug, "slug")`)— Agent slug 只做「非空 + Trim」,沒有任何格式/長度驗證:`"../../etc"`、含空白、含中文、8000 字元的 slug 目前一律 201 建立成功(slug 進 UNIQUE(tenant_id, slug) 與未來的 catalog 路由)。兩個候選行為:(A) 比照 `AgentAudience` 的 canonical group id 加上 regex + 長度上限並回 400(風險:既有已建立的 Agent slug 若不合規,之後每次 PUT draft/publish 都會失敗,屬追溯破壞;且 slug 不可改,只能重建);(B) 明示「slug 是自由字串,唯一性由 DB 保證、路由一律用 uuid」並補測試釘住這個意圖(風險:未來若 USER catalog 用 slug 當公開路徑鍵,格式問題會在那時才爆,且無第二道牆)。本輪只補了 `Create_BlankSlugOrName_Returns400`(已定案的空白分支),刻意不寫「任意 slug 皆接受」的測試把現狀固化成契約。**結案**:採 A,但**只在 create 驗證**,A 案原列的追溯破壞風險因此不成立 —— slug 是 immutable(`AgentDtos.cs:10` 明寫 PUT 忽略 slug、所有 UPDATE 語句不含該欄、canonical 定義排除它),而 validate/publish/restore 驗的是 `Name` 不是 `Slug`,所以既有不合規的 Agent 不會在任何其他路徑上被打爆。作法:`Create` 改呼叫 `RequireCanonicalSlug`,在既有 `Require`(非空 + Trim)之後直接重用 `AgentAudience.IsCanonicalGroupId`(同一條 regex `^[a-z0-9](?:[a-z0-9._-]{0,126}[a-z0-9])?\z` + `MaxGroupIdLength=128`),不合格回 400「slug 格式不正確」+ 既有 ApiError 形狀。不另開 `AgentSlugRules` —— canonical id 的形狀規則已經有一份,再寫一份就是同一條 regex 兩處維護。測試:`Create_MalformedSlug_Returns400`(路徑片段 `../../etc`、含空白、中文、大寫、前後綴 `-` 共六格)+ `Create_SlugLengthBoundary_AcceptsMaxRejectsMaxPlusOne`(128 通過 / 129 拒絕,on-point 與 off-point 各一)。**副作用**:四個既有測試(`Validate_InvalidDraft_ReturnsErrors_AndDoesNotMarkValidated` ×2、`NonAdmin_Write_Returns403` ×2)的 fixture slug 由 theory 參數拼成,含大寫/`[]`/結尾 `-`,新規則下 create 直接 400。它們斷言的是 draft 驗證與角色把關,slug 只是為了讓每測不撞名的識別標籤,故改為經測試內新增的 `Slug()` 正規化;沒有放寬任何一條斷言。
